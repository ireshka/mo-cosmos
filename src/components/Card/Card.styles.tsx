import styled from 'styled-components';

export const Container = styled('div')`
  border-radius: 20px;
  overflow: hidden;
  position: relative;
`;

export const Image = styled('img')`
  display: block;
  width: 100%;
`;
